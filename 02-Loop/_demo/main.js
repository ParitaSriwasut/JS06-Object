const user = {
  firstName: "Parita",
  age: 16,
  address: "Thailand",
};
//for in : Loop through Object.
//for(let key variable / in / obj name)
for (let key in user) {
  //console.log(key);
  console.log(user[key]);
}
